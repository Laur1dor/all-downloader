# Secrets and runtime data
.env
data/
logs/
*.session
# WireGuard/AmneziaWG configs contain private keys
*.conf

# Personal / generated data — not part of the source
info.txt
db_backup.sql
*.sql
deploy.tgz
*.zip
*.rar

# Separate, unrelated private project — must never be part of this repo
tgtime/

# Personal operations guide — only ever goes to the PRIVATE repo (force-added)
PRIVATE-GUIDE.md

# Private context/handoff artifact — never publish (contains infra/secrets refs)
PROJECT-CONTEXT.md

# Full feature inventory — infra details and the whole platform list; private only
FEATURES.md

# Python / tooling
.venv/
__pycache__/
*.pyc
.ruff_cache/
data/awg/
