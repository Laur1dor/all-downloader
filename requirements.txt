aiogram==3.31.0
gallery-dl==1.32.9  # photo posts (Instagram/TikTok) that yt-dlp cannot handle
curl_cffi==0.16.2  # browser TLS impersonation for sites that block by fingerprint (PornHub)
aiohttp==3.14.3  # newest version allowed by aiogram 3.31.x (<3.15)
aiohttp-socks==0.10.1  # aiohttp speaks no SOCKS on its own; pool health checks need it
asyncpg==0.31.0
python-dotenv==1.2.3
yt-dlp==2026.8.19
PySocks==1.7.1  # SOCKS proxy support for gallery-dl (VLESS fallback routing)
